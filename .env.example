# =============================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this to .env and fill in actual values: cp .env.sample .env
# =============================================================================

# Application environment: development, production
NODE_ENV=

# MongoDB initialization (only needed for Docker container setup, not for Atlas)
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
MONGO_INITDB_DATABASE=

# Database connection URLs
MONGO_URL_DEV=
MONGO_URL_PROD=
MONGO_URL_TEST=

# =============================================================================
# EXAMPLE VALUES
# =============================================================================
# FOR LOCAL DOCKER DEVELOPMENT:
# NODE_ENV=development
# MONGO_INITDB_ROOT_USERNAME=mongouser
# MONGO_INITDB_ROOT_PASSWORD=SecurePassword123!
# MONGO_INITDB_DATABASE=hcx_app
# MONGO_URL_DEV=mongodb://mongouser:SecurePassword123!@localhost:27017/hcx_app?authSource=admin
# MONGO_URL_TEST=mongodb://testuser:testpass123@localhost:27018/hcx_app_test?authSource=admin

# FOR PRODUCTION WITH ATLAS:
# NODE_ENV=production
# MONGO_URL_PROD=mongodb+srv://produser:SuperSecurePass456@cluster0.abc123.mongodb.net/hcx_app_prod?retryWrites=true&w=majority
# (MONGO_INITDB_* variables not needed for Atlas)